<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/choose_reward_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/android:colorBackground"
    android:fitsSystemWindows="true">

    <!-- Top AppBar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        style="@style/TopAppBarStyle"
        app:title="Choose Reward"
        app:navigationIcon="@drawable/ic_back"
        app:navigationContentDescription="@string/back"
        app:navigationIconTint="?attr/android:textColorSecondary"/>

    <!-- Content -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/reward_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:clipToPadding="false"
        android:padding="24dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <!-- Rewards Row -->
            <LinearLayout
                android:id="@+id/reward_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal"
                android:layout_marginTop="40dp"
                android:layout_marginBottom="40dp">

                <ImageView
                    android:id="@+id/reward1"
                    android:layout_width="100dp"
                    android:layout_height="140dp"
                    android:layout_marginEnd="16dp"
                    android:src="@drawable/streak_shield"
                    android:contentDescription="Reward 1"
                    android:scaleType="centerCrop" />

                <ImageView
                    android:id="@+id/reward2"
                    android:layout_width="100dp"
                    android:layout_height="140dp"
                    android:layout_marginEnd="16dp"
                    android:src="@drawable/damage_boost"
                    android:contentDescription="Reward 2"
                    android:scaleType="centerCrop" />

                <ImageView
                    android:id="@+id/reward3"
                    android:layout_width="100dp"
                    android:layout_height="140dp"
                    android:src="@drawable/extra_swing"
                    android:contentDescription="Reward 3"
                    android:scaleType="centerCrop" />
            </LinearLayout>

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/button_confirm_reward"
                style="@style/PrimaryButton"
                android:text="Confirm"
                android:layout_marginBottom="8dp"/>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
