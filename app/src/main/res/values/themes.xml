<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MineYourMoney" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Backgrounds -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:colorForeground">@color/foreground_light</item>
        <!-- Text Colours -->
        <item name="android:textColorPrimary">@color/text_primary_light</item>
        <item name="android:textColorSecondary">@color/text_secondary_light</item>

        <item name="android:calendarTextColor">@color/text_primary_dark</item>
    </style>

    <style name="Theme.MineYourMoney" parent="Base.Theme.MineYourMoney" />
</resources>